import classes from '../../style/chat/SideBar.module.css'

const SideBar = () => {

    return (
        <section style={classes}>
            <h1>SideBar</h1>
        </section>
    );
}

export default SideBar;